services:
  - type: web
    name: conexao-fitness-backend
    env: docker
    plan: free
    region: oregon
    rootDir: conexao-fitness-backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production

  - type: static
    name: conexao-fitness-frontend
    plan: free
    region: oregon
    rootDir: conexao-fitness-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  - type: web
    name: conexao-fitness-web
    env: node
    plan: free
    region: oregon
    rootDir: conexao-fitness-web
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
